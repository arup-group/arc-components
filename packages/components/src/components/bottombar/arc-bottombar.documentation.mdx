import { Meta, Primary, Controls, Canvas } from '@storybook/blocks';
import * as BottombarStories from './arc-bottombar.stories';

<Meta title="Components/ArcBottombar/Documentation" />

# ArcBottombar

The `ArcBottombar` component is included with the the the `ArcContainer` component by default.

<Primary />
