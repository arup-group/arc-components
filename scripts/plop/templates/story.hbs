import { Meta, Story } from '@storybook/web-components';
import { html } from 'lit';
import type {{ properCase tag }} from './{{ properCase tag }}.js';
import './{{ tag }}.js';

export default {
  title: 'Components/{{ properCase tag }}',
  component: '{{ tag }}'
} as Meta;

const Template: Story<{{ properCase tag }}> = ({ name, active }) => html`
  <{{ tag }} name=${name} ?active=${active}></{{ tag }}>
`;

const defaultArgs = {
  name: 'Test component',
  active: false,
};

export const Default = Template.bind({});
Default.args = { ...defaultArgs };

export const Active = Template.bind({});
Active.args = { ...defaultArgs, active: true };
