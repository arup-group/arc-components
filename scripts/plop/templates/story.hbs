import { Meta } from '@storybook/web-components';
import { html, TemplateResult } from 'lit';
import {{ properCase tag }} from './{{ properCase tag }}.js';

interface Story<T> {
  (args: T): TemplateResult;
  args?: Partial<T>;
  argTypes?: Record<string, unknown>;
}

interface ArgTypes {
  name: string,
  active: boolean,
}

export default {
  title: 'Components/{{ properCase tag }}',
  component: `${{{ properCase tag }}.tag}}`
} as Meta;

const Template: Story<ArgTypes> = ({ name, active }: ArgTypes) => html`
  <{{ tag }} name="${name}" ?active="${active}"></{{ tag }}>
`;

const defaultArgs: ArgTypes = {
  name: 'Hello World',
  active: false,
};

/* TYPES */
export const Default = Template.bind({});
export const CustomColor = Template.bind({});
Default.args = { ...defaultArgs, active: true };

/* STATES */
export const Active = Template.bind({});
Active.args = { ...defaultArgs, label: 'Active', active: true };
