import { ContainerTheme } from '../../container/constants/ContainerConstants.js';

export declare type colourMode = {
  theme: ContainerTheme
}

export const ACCESSIBILITY_SETTINGS: {} = {
  colourMode: {
    theme
  }
}
