import { Meta, Story, Canvas, ArgsTable } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import * as stories from '../out-tsc/components/button/arc-button.stories.js';
import {
  BUTTON_COLORS,
  BUTTON_SIZES,
  BUTTON_TYPES,
} from '../out-tsc/components/button/constants/ButtonConstants.js';

<Meta title='ArcButton' component='arc-button' argTypes={{
  type: {
    description: 'Set the type of the button.',
    defaultValue: { summary: 'contained' },
    control: { type: 'select' },
    options: Object.keys(BUTTON_TYPES),
  },
  color: {
    description: 'Set the color of the button.',
    defaultValue: { summary: 'default' },
    control: { type: 'select' },
    options: Object.keys(BUTTON_COLORS),
  },
  size: {
    description: 'Set the size of the button.',
    defaultValue: { summary: 'medium' },
    control: { type: 'select' },
    options: Object.keys(BUTTON_SIZES),
  },
  href: {
    description: 'When set, the underlying button will be rendered as an anchor with this property.',
    control: { type: 'text' },
  },
  target: {
    description: 'Tells the browser where to open the link. Only used when href is set.',
    control: { type: 'text' },
  },
  download: {
    description: 'Tells the browser to download the linked file as this filename. Only used when href is set.',
    control: { type: 'text' },
  },
  active: {
    description: 'Draws the button in an active state.',
    defaultValue: { summary: 'false' },
    control: { type: 'boolean' },
  },
  disabled: {
    description: 'Draws the button in a disabled state.',
    defaultValue: { summary: 'false' },
    control: { type: 'boolean' },
  },
  loading: {
    description: 'Draws the button in a loading state.',
    defaultValue: { summary: 'false' },
    control: { type: 'boolean' },
  },
  submit: {
    description: 'Indicates if activating the button should submit the form. Ignored when href is set.',
    defaultValue: { summary: 'false' },
    control: { type: 'boolean' },
  },
  width: {
    type: { required: false },
    description: 'Set the width of the button.',
    defaultValue: { summary: 'auto' },
    control: { type: 'text' },
    table: {
      category: 'CSS Variables',
    },
  },
  minWidth: {
    name: '--min-width',
    type: { required: false },
    description: 'Set the min width of the button.',
    defaultValue: { summary: '0' },
    control: { type: 'text' },
    table: {
      category: 'CSS Variables',
    },
  },
  btnColor: {
    name: '--btn-color',
    type: { required: false },
    description: 'Overwrite the font color of the button.',
    control: { type: 'color' },
    table: {
      category: 'CSS Variables',
    },
  },
  btnBackground: {
    name: '--btn-background',
    type: {required: false},
    description: 'Overwrite the background color of the button.',
    control: {type: 'color'},
    table: {
      category: 'CSS Variables',
    },
  },
}} />

# Button
### `<arc-button>`

The ArcButton represents actions that are available to the user.

## Examples
### Types
Use the `type` attribute to set the button type.
<Canvas>
  <Story name='Contained' story={stories.Contained} />
  <Story name='Tile' story={stories.Tile} />
  <Story name='Outlined' story={stories.Outlined} />
  <Story name='Pill' story={stories.Pill} />
  <Story name='Tab' story={stories.Tab} />
</Canvas>

### Colors
Use the `color` attribute to set the button's color.
<Canvas>
  <Story name='Default' story={stories.Default} />
  <Story name='Primary' story={stories.Primary} />
  <Story name='Secondary' story={stories.Secondary} />
  <Story name='Error' story={stories.Error} />
  <Story name='Warning' story={stories.Warning} />
  <Story name='Info' story={stories.Info} />
  <Story name='Success' story={stories.Success} />
</Canvas>

### Sizes
Use the `size` attribute to set the button's size
<Canvas>
  <Story name='Small' story={stories.Small} />
  <Story name='Medium' story={stories.Medium} />
  <Story name='Large' story={stories.Large} />
</Canvas>

### Link button
It's often helpful to have a button that works like a link.
This is possible by setting the `href` attribute, which will make the component render an `<a>` under the hood.
This gives all the default link behavior the browser provides (e.g. CMD/CTRL/SHIFT + CLICK) and exposes the target and download attributes.

<Canvas>
  <Story name='Link' story={stories.Link} />
  <Story name='LinkNewWindow' story={stories.LinkNewWindow} />
  <Story name='LinkDownload' story={stories.LinkDownload} />
  <Story name='LinkDisabled' story={stories.LinkDisabled} />
</Canvas>

### States
Use the `active`, `disabled` or `loading` attributes to change the state of the button.

<Canvas>
  <Story name='Active' story={stories.Active} />
  <Story name='Disabled' story={stories.Disabled} />
  <Story name='Loading' story={stories.Loading} />
</Canvas>

### Prefix and Suffix icons
Use the `prefix` and `suffix` slots to add icons.

<Canvas>
  <Story name='Prefix' story={stories.Prefix} />
  <Story name='Suffix' story={stories.Suffix} />
</Canvas>

## Properties
<ArgsTable story='Contained' />

## Events
Coming soon

## Methods

| Name         | Description                                               |
|--------------|-----------------------------------------------------------|
| click        | Simulates a click on the button.                          |

## Slots

| Name         | Description                                               |
|--------------|-----------------------------------------------------------|
| (default)    | The button's label.                                       |
| prefix       | Used to prepend an icon or similar element to the button. |
| suffix       | Used to append an icon or similar element to the button.  |

## Dependencies

This component imports the following dependencies.
* [arc-spinner](?path=/story/arcspinner--default)
