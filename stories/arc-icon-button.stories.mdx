import { Meta, Story, Canvas, ArgsTable } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import * as stories from '../out-tsc/components/icon-button/arc-icon-button.stories.js';
import { ICON_TYPES } from '../out-tsc/components/icon/constants/IconConstants.js';

<Meta
title='ArcIconButton'
component='arc-icon-button'
argTypes={{
    name: {
      description: 'Set the type of the icon.',
      defaultValue: { summary: ICON_TYPES.fire },
      control: { type: 'select' },
      options: Object.values(ICON_TYPES),
    },
    label: {
      description: 'A description that gets read by screen readers and other assistive devices. For optimal accessibility, you should always include a label that describes what the icon button does.',
      control: { type: 'text' },
    },
    href: {
      description: 'When set, the underlying button will be rendered as an anchor with this property.',
      control: { type: 'text' },
    },
    target: {
      description: 'Tells the browser where to open the link. Only used when href is set.',
      control: { type: 'text' },
    },
    download: {
      description: 'Tells the browser to download the linked file as this filename. Only used when href is set.',
      control: { type: 'text' },
    },
    active: {
      description: 'Draws the button in an active state.',
      defaultValue: { summary: 'false' },
      control: { type: 'boolean' },
    },
    disabled: {
      description: 'Draws the button in a disabled state.',
      defaultValue: { summary: 'false' },
      control: { type: 'boolean' },
    },
    loading: {
      description: 'Draws the button in a loading state.',
      defaultValue: { summary: 'false' },
      control: { type: 'boolean' },
    },
    iconColor: {
        name: '--icon-color',
        type: { required: false },
        description: 'Overwrite the color of the icon.',
        control: { type: 'color' },
        table: {
            category: 'CSS Variables',
        },
    },
}} />

# ArcIconButton
### `<arc-icon-button>`

The ArcIconButton is a simple, icon-only button that can be used for actions and in toolbars.
For a full list of icons that come bundled with Arc, refer to the #link input here.

## Examples
### Colors
Icon buttons are designed to have a uniform appearance, so their color is not inherited.
However, you can still customize them by styling the css variables.
<Canvas>
  <Story name='Default' story={stories.Default} />
  <Story name='CustomColor' story={stories.CustomColor} />
</Canvas>

### Link button
It's often helpful to have a button that works like a link.
This is possible by setting the `href` attribute, which will make the component render an `<a>` under the hood.
This gives all the default link behavior the browser provides (e.g. CMD/CTRL/SHIFT + CLICK) and exposes the target and download attributes.

<Canvas>
  <Story name='Link' story={stories.Link} />
  <Story name='LinkNewWindow' story={stories.LinkNewWindow} />
  <Story name='LinkDownload' story={stories.LinkDownload} />
  <Story name='LinkDisabled' story={stories.LinkDisabled} />
</Canvas>

### States
Use the `active`, `disabled` or `loading` attributes to change the state of the button.

<Canvas>
  <Story name='Active' story={stories.Active} />
  <Story name='Disabled' story={stories.Disabled} />
  <Story name='Loading' story={stories.Loading} />
</Canvas>

## Properties
<ArgsTable story='Default' />

## Events
Coming soon

## Methods

| Name         | Description                                               |
|--------------|-----------------------------------------------------------|
| click        | Simulates a click on the button.                          |

## Slots

| Name         | Description                                               |
|--------------|-----------------------------------------------------------|
| (default)    | The button's label.                                       |

## Dependencies

This component imports the following dependencies.
* [arc-icon](?path=/story/arcicon--default)
* [arc-spinner](?path=/story/arcspinner--default)
