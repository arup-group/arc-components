import { Meta, Story, Canvas, ArgsTable } from '@web/storybook-prebuilt/addon-docs/blocks.js';
import * as stories from '../out-tsc/stories/arc-icon.stories.js';
import { ICON_SIZES, ICON_TYPES } from '../out-tsc/src/components/icon/constants/IconConstants.js';

<Meta
title='ArcIcon'
component='arc-icon'
argTypes={{
    name: {
      description: 'Set the type of the icon.',
      defaultValue: { summary: 'fire' },
      control: { type: 'select' },
      options: Object.values(ICON_TYPES),
    },
    size: {
      description: 'Set the size of the icon.',
      defaultValue: { summary: 'medium' },
      control: { type: 'select' },
      options: Object.values(ICON_SIZES),
    },
    rotation: {
      description: 'Set the rotation of the icon.',
      defaultValue: { summary: '0' },
      control: { type: 'number' },
    },
    spinning: {
      description: 'Draws the icons in a spinning state.',
      defaultValue: { summary: 'false' },
      control: { type: 'boolean' },
    },
    colorPrimary: {
        name: '--icon-color-primary',
        type: { required: false },
        description: 'Set the primary color of the icon.',
        control: { type: 'color' },
        table: {
            category: 'CSS Variables',
        },
    },
    colorSecondary: {
        name: '--icon-color-secondary',
        type: { required: false },
        description: 'Set the secondary color of the icon (if supported).',
        control: { type: 'color' },
        table: {
            category: 'CSS Variables',
        },
    },
}} />

# ArcIcon
### `<arc-icon>`

The ArcIcon is a symbol that can be used to represent various options within an application.

<Canvas>
  <Story name='Default' story={stories.Default} />
</Canvas>

## Examples
### Colors
Icons are designed to have a uniform appearance, so their color is not inherited.
However, you can still customize them by styling the css variables.
<Canvas>
  <Story name='RedColor' story={stories.RedColor} />
  <Story name='GreenColor' story={stories.GreenColor} />
  <Story name='BlueColor' story={stories.BlueColor} />
  <Story name='PurpleColor' story={stories.PurpleColor} />
</Canvas>

### Sizing
Icons are sized using the --arc-font-size-{size} css variable, where {size} is a lowercase value representing the size of the font.
There are two ways to set their size. The easiest way is to use one of the built-in theme variables as shown below:

```bash
--arc-font-size-xx-small
--arc-font-size-x-small
--arc-font-size-small
--arc-font-size-medium
--arc-font-size-large
--arc-font-size-x-large
--arc-font-size-xx-large
--arc-font-size-xxx-large
--arc-font-size-xxxx-large
```

```bash
<arc-icon name='fire' size='xxxx-large'></arc-icon>
```

which results in the following

<Canvas>
  <Story name='VariableSize' story={stories.VariableSize} />
</Canvas>

or create your own custom css variable that is scoped to :root as shown below.

```bash
<html>
  <head>
    <style>
      :root {
        --arc-font-size-custom: 4.5rem;
      }
    </style>
  </head>

  <body>
    <arc-icon name='fire' size='custom'></arc-icon>
  </body>
</html>
```

which results in the following

<Canvas>
  <Story name='CustomSize' story={stories.CustomSize} />
</Canvas>

## Properties
<ArgsTable story='Default' />
